{
  "name": "apollo-server-graphql-starter",
  "version": "1.0.0",
  "description": "A lightweight GraphQL API starter pack",
  "keywords": [
    "express",
    "API",
    "MongoDB",
    "mongoose",
    "GraphQL",
    "Nginx"
  ],
  "license": "MIT",
  "url": "https://github.com/tomanagle/Apollo-Server-GraphQL-starter",
  "author": "Tom Nagle",
  "email": "tomanagle@gmail.com",
  "scripts": {
    "dev:server": "npm run --prefix server dev",
    "dev:frontend": "npm run --prefix frontend dev",
    "deploy": "npm run --prefix server deploy && npm run --prefix frontend deploy",
    "setup": "npm run --prefix server setup",
    "test:server": "npm run --prefix server test",
    "test:frontend": "npm run --prefix frontend test",
    "test": "npm run --prefix server test && npm run --prefix frontend test"
  }
}
